# Kaze config — lives at ${CONFIG_DIR}/kaze/config.yml on the host.
# Reference: pixelotes/kaze on Docker Hub.

app:
  port: 4533
  ui_password: "REPLACE_WITH_STRONG_PASSWORD"
  jwt_secret: "REPLACE_WITH_LONG_RANDOM_STRING"   # openssl rand -hex 48

libraries:
  - friendly_name: "Music"
    path: "/app/media"

# Local environment
.env
.env.*
!.env.example

# Per-service runtime state — bind-mounted into containers and grows on use.
# Each block keeps the tracked config and ignores everything else.
tango/session/
tango/data/
reel/data/
rms/config/*
!rms/config/config.yml
suika/config/*
!suika/config/config.yml
scarf/data/
navidrome/

# Logs
*.log
*.log.*

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
*.tmp

# Local scratch dir
temp/
